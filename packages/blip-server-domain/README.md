# Blip Server Domain

Runtime-independent contracts shared by Blip Server implementations. This
package contains API schemas, domain models and errors, Effect service tags,
repository interfaces, and policy definitions. It does not contain service
layers, persistence queries, or deployment-specific code.
